## Workflow

- always run test after each meaningful change: `bb test` OR with `cargo` directly
- after each feature implementation, run `bb lint` and `bb fmt`
- in the end of the plan execution run `bb build`
